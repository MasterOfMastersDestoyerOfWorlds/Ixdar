/**
 * Two coexisting menu kinds: the `Menu`/`MenuBox`/`MenuItem` screen-stack system, and
 * `SceneModelMenu`, an immediate-mode ESC panel rebuilt every frame for live highlights and
 * clickable regions.
 */
package ixdar.gui.ui.menu;
