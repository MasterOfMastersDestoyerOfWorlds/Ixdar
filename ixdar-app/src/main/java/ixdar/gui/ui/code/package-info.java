/**
 * The live GLSL editor view: `ShaderCodePane` (scrollable pane), `GLSLColorizer` (highlighting),
 * `ShaderBranchInjector` (rewrites shader source to inject debug preview writes). Evaluation lives
 * in `ixdar.parsing.glsl`.
 */
package ixdar.gui.ui.code;
