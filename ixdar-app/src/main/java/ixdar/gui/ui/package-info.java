/**
 * `Drawing`: static low-level 2D overlay drawing (segments, circles, paths, knot diagrams), cached
 * per platform id. Its statics (thickness, font size) are retuned at runtime with global visual
 * effect.
 */
package ixdar.gui.ui;
