/**
 * Command-pattern adapters between menu rows and effects (`Action.perform()`): screen switches,
 * `.ix` loading, map editor. Mostly delegates to terminal commands; `StartNewGameAction` carries
 * real game-setup logic.
 */
package ixdar.gui.ui.actions;
