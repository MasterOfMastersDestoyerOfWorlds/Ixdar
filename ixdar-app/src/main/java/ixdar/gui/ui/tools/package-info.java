/**
 * Mode-like editor tools subclassing `Tool`: free inspection, map editing, diagnostic overlays,
 * plus two trade-game tools (`HeadquartersPickerTool`, `RoutePlanningTool`) that depend on the game
 * model instead of the knot editor. `Tool.draw` throws unless overridden.
 */
package ixdar.gui.ui.tools;
