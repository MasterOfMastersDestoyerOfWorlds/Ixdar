/**
 * Trade-game domain model: `City`, `Road`, and `CityNetwork` (all possible routes, distinct from
 * the player's `Knot` trade routes). Model classes carry their own drawing code.
 * `IrregularQuadLayoutGenerator` (Townscaper-style quad layouts) lives here but belongs with
 * geometry.
 */
package ixdar.game;
