/**
 * Scene layer root. `Scene` extends `Canvas3D`; `ModelScene` adds mesh viewing (framing, orbit
 * preservation, model catalog, direct input binding). Scenes register via `@SceneAnnotation` into
 * the generated scene registry.
 */
package ixdar.scenes;
