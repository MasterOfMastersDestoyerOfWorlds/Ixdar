/**
 * Ring authoring: `RingScene` (scene id `ring-tool`) extends the mesh viewer with the
 * hover-to-preview `RingTool`, the `ring` terminal command's waypoints, and the numbered
 * thick-loop overlay the graph's `ring_NN` edge-mark labels draw as.
 */
package ixdar.scenes.ring;
