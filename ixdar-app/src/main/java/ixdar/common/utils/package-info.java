/**
 * Small static helpers. Only `Compat` is general (null-safe string shims and an `fmaf` fallback
 * that rounds twice; not IEEE fused); `Utils` and `RunListUtils` are knot-algorithm helpers.
 * `StringBuff` is a depth-tagged debug trace.
 */
package ixdar.common.utils;
