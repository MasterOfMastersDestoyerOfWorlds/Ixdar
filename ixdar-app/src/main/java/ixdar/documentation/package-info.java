/**
 * Repo documentation tooling. `SpliceArchitectureDoc` runs at every compile and splices all
 * `package-info.java` descriptions into the root `ARCHITECTURE.md` between markers, failing the
 * build when the markers are missing.
 */
package ixdar.documentation;
