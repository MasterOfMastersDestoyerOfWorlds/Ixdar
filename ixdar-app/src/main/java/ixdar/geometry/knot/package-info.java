/**
 * The recursive knot structure (`Knot` is a circular route of knots bottoming out at points) plus
 * undo/redo records for route planning. Fused to the 2D renderer: `Knot extends SDFCircle`,
 * `Segment extends SDFLine`, both import `MainScene`.
 */
package ixdar.geometry.knot;
