/**
 * Split point between two disjoint bodies of geometry code: `mesh` (the current node-graph stack)
 * and the TSP lineage (`cuts`, `knot`, `point`, `shell`). Verified: nothing under `mesh` imports
 * the TSP packages.
 */
package ixdar.geometry;
