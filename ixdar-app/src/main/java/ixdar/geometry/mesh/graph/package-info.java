/**
 * Node-graph runtime: `NodeGraphRuntime` parses and executes .dsl sources against the generated
 * registries, with the `Class.forName` desktop firewall, per-node timing (`logTimings`), and
 * `GraphAnalyzer` introspection.
 */
package ixdar.geometry.mesh.graph;
