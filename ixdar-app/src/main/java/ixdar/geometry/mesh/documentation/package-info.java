/**
 * Mesh-node catalog generation: `MeshNodeCatalog` exports all node metadata to the checked-in JSON
 * every compile (sorted, byte-stable); DSL validation entry points.
 */
package ixdar.geometry.mesh.documentation;
