/**
 * Mesh-adjacent data contracts: `GeometryBundle` (mesh plus named slots), `EdgeKey` packing,
 * `EdgeMarks`, `CurveGeometry`, decomposition and feature-detection code (Morse-Smale, crest lines,
 * semantic patches). The conventions in ARCHITECTURE.md's constraints section are enforced from
 * here.
 */
package ixdar.geometry.mesh.data;
