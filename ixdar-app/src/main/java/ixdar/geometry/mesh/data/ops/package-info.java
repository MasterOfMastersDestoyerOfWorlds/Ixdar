/**
 * Representation-preserving mesh operations (delete vertices/edges, merge by distance, vertex
 * offset). Each branches on the concrete mesh type and returns the input's kind.
 */
package ixdar.geometry.mesh.data.ops;
