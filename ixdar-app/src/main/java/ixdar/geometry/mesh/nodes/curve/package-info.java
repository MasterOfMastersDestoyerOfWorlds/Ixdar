/**
 * Curve nodes: primitives (circle, bezier, function), conversion to and from meshes, sweep, loft,
 * resample, deform. Curves travel between nodes as `CurveGeometry` under `CurveGeometry.SLOT`.
 */
package ixdar.geometry.mesh.nodes.curve;
