/**
 * Data-attachment nodes: tagging geometry (`TagGeometryNode` owns the `_tags` slot), named
 * attributes, and inputs that surface external data to a graph.
 */
package ixdar.geometry.mesh.nodes.data;
