/**
 * Graph nodes exposing the quad-layout pipeline stages as independent
 * capabilities: cross field, seamless parametrization, and motorcycle-graph
 * tracing, with more stages to follow per the 7.2 migration. All desktop-only;
 * the stages lean on native solver backends the web platform refuses.
 */
package ixdar.geometry.mesh.nodes.quadlayout;
