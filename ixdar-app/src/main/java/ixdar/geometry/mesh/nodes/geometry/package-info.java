/**
 * Geometry-level operations: mesh boolean (desktop-only, Manifold FFM backend; owns the provenance
 * slots), join, transform-level ops on whole bundles.
 */
package ixdar.geometry.mesh.nodes.geometry;
