/**
 * The ~95 DSL mesh nodes, organized by category subpackage. Every node follows the `GridMeshNode`
 * shape: public static port constants with inline name literals, `socketDocs` keyed by `PORT.name`,
 * documentation enforced by `MeshNodeRegistryTest`.
 */
package ixdar.geometry.mesh.nodes;
