/**
 * Coons-patch machinery: patch fill, extrude and inset with bezier handle preservation
 * (`CoonsHandleBuilder`, handle slots owned by `AssignBezierHandlesNode`).
 */
package ixdar.geometry.mesh.nodes.patch;
