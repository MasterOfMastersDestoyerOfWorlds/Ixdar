/**
 * The mesh-node contract surface: {@code MeshNode},
 * {@code InputPort}/{@code OutputPort}/{@code PortType}, typed fields,
 * {@code NodeContext}, {@code MapNodeContext}, {@code CanonicalPortNames}, and
 * the port value interfaces. Moved from the annotations module so port types
 * can reference app classes; only the annotation and its processor stay there.
 */
package ixdar.geometry.mesh.nodes.api;
