/**
 * Primitive generators: cube, grid, icosphere, UV sphere, cylinder, cone, disk, torus, segments.
 * `GridMeshNode` is the reference node shape and exposes static helpers (`triangulated`,
 * `vertexId`, `rowCoordinate`) that tests build fixture geometry with.
 */
package ixdar.geometry.mesh.nodes.primitives;
