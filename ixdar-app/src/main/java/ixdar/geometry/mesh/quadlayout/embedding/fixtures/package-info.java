/**
 * Hand-built embedded T-mesh fixtures (torus, plane, pinch cases) used by the embedding unit tests.
 * Fixture-to-DSL conversion is deferred until the pipeline is nodes.
 */
package ixdar.geometry.mesh.quadlayout.embedding.fixtures;
