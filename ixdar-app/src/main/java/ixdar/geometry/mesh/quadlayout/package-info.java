/**
 * Desktop-only quad-layout pipeline, cross field to quantized quad layout (BZK09/LCBK19 lineage).
 * Slated for integration into the node system; until then it is reached from scenes, not from DSL
 * graphs.
 */
package ixdar.geometry.mesh.quadlayout;
