/**
 * `ExportAutomationRoutes` writes the route manifest JSON every compile; the checked-in
 * `automation_routes.json` is generated output and the single source of truth for the Python CLI.
 */
package ixdar.platform.automation.documentation;
