/**
 * View observation and posing: screenshots (single and multiview), orbit and projection get/set
 * (paired classes sharing a path, differing by HTTP method), and the full UI state dump.
 */
package ixdar.platform.automation.endpoints.ui;
