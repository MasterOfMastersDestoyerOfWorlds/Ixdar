/**
 * Ring routes: the ranked list of neck rings a mesh's skeleton suggests, and ring authoring
 * through surface coordinates so an agent can cut a limb without clicking. Authoring needs the
 * {@code ring-tool} scene, whose overlay also draws the numbered render {@code ui/multiview}
 * captures for a vision model.
 */
package ixdar.platform.automation.endpoints.mesh.rings;
