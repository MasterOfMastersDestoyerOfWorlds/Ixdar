/**
 * Synthesized input routes (`/input/click`, `/input/key`, `/input/type`, scroll, hover) that feed
 * the active `MouseTrap`/`KeyGuy` directly, bypassing the OS event source.
 */
package ixdar.platform.automation.endpoints.input;
