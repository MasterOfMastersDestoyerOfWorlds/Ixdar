/**
 * Portability root: `Platforms` is the process-global registry (`init`, `get()`, `gl()`, `switchTo`
 * for multi-canvas web). `Toggle` is the app-wide feature-flag enum, here only because everything
 * reaches for it.
 */
package ixdar.platform;
