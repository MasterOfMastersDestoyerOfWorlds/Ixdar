/**
 * Thin `MeshNode` adapters wrapping each algorithm stage for the DSL, all in the `dungeon` scope:
 * 2D and 3D variants of rooms, Delaunay, MST, corridors, and grid-to-mesh.
 */
package ixdar.procgen.dungeon.nodes;
