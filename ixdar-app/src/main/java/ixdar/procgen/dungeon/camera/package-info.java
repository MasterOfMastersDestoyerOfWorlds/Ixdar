/**
 * `ThirdPersonCamera` (orbit above the player, writes back into the shared `Camera3D`) and
 * `CameraGridSweep` (sphere-cast that hard-stops at the first obstacle cell).
 */
package ixdar.procgen.dungeon.camera;
