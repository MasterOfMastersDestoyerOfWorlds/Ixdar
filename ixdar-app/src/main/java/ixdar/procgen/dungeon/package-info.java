/**
 * Vazgriz-style dungeon pipeline in 2D and 3D: place rooms, Delaunay-triangulate, MST plus loop
 * edges, A* corridors, tile grid to mesh; exposed as DSL nodes; plus the runtime to walk the
 * result (capsule physics, player controller, cameras, viewer scene).
 */
package ixdar.procgen.dungeon;
