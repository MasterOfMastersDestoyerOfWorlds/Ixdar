/**
 * `PlayerController` (WASD, gravity, yaw-relative motion; mouse-look belongs to the mouse handler),
 * `PlayerSpawner` (relies on room[0] being the start room), `SpawnPoint`.
 */
package ixdar.procgen.dungeon.player;
