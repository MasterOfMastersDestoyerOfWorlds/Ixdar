/**
 * Viewer scene(s) that run a dungeon {@code .dsl} via {@code NodeGraphRuntime} and render the
 * output mesh with a WASD + mouse-look fly camera. Reuses existing Camera3D / KeyGuy / MouseTrap
 * plumbing — no new input infrastructure. Implementation lands in PROCGEN-6.
 */
package ixdar.procgen.dungeon.scene;
