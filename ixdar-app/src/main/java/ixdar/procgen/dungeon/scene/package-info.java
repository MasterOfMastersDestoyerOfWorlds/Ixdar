/**
 * Viewer wiring: `DungeonViewerScene` (F toggles fly-cam vs player-walk, V toggles person view;
 * only 3D dungeons support player mode since the DSL mesh feeds both renderer and collision),
 * `DungeonKeyGuy`, `FlyCamMouseTrap`.
 */
package ixdar.procgen.dungeon.scene;
