/**
 * GLSL tokenizer/interpreter feeding the live shader editor; uniforms, declarations, branches,
 * built-in constants. An interpreter for the debug UI, not a compiler; depends on the color and
 * text render packages.
 */
package ixdar.parsing.glsl;
