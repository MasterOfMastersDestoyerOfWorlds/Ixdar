/**
 * Signed-distance-field drawables, the editor's 2D drawing vocabulary: `ShaderDrawable` base,
 * `SDFLine` variants, circles, textures (MSDF). `SDFUnion` imports LWJGL directly; the one GL-
 * abstraction violation in `graphics` outside `render/model`.
 */
package ixdar.graphics.render.sdf;
