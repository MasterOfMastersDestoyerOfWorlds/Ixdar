/**
 * MSDF font atlas loading and `HyperString`, the colored, wrappable, hoverable, clickable rich-text
 * model used for all editor UI text. Each glyph is an SDF drawable. Private-Use-Area code points
 * carry non-font glyphs.
 */
package ixdar.graphics.render.text;
