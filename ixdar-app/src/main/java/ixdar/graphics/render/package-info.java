/**
 * Shared render primitives: `Clock` (process-global static time, frame deltas, oscillation; the
 * animation driver for shaders) and `Texture` (supports a deferred placeholder mode filled in by
 * async platform loading).
 */
package ixdar.graphics.render;
