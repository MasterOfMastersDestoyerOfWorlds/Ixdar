/**
 * `Camera2D` (pan/scale, zoom-to-fit) and `Camera3D` (first-person plus orbit) behind a 2D-shaped
 * `Camera` interface; several `Camera3D` methods throw. `Bounds` is the viewport rectangle with a
 * resize recalculator.
 */
package ixdar.graphics.cameras;
