/**
 * The mesh-node contract surface: `MeshNode`, `InputPort`/`OutputPort`/`PortType`, typed fields,
 * `NodeContext`, `MapNodeContext`, `CanonicalPortNames`. The one RUNTIME-retained annotation, so
 * the catalog can filter by scope. Dungeon port types are `Object.class` holes pointing at classes
 * in the app module.
 */
package ixdar.annotations.meshnode;
