/**
 * HTTP route declaration: `@AutomationRouteAnnotation` plus the `RouteDoc` machine-readable docs.
 * Registry is keyed by id; two routes with the same simple class name silently overwrite unless
 * ids differ.
 */
package ixdar.annotations.automation;
