/**
 * The annotation-processor module.
 * `RegistryProcessor` is the shared base: each registry generates an id-to-supplier map class, with
 * a `Desktop`-suffixed variant the web build never references.
 */
package ixdar.annotations;
