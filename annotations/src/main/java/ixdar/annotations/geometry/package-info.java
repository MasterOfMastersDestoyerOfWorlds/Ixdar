/**
 * The smallest registry: `@GeometryAnnotation` over the bare `Geometry` marker. Serves the legacy
 * 2D CLI geometries; the real parse/serialize contract lives app-side in `PointCollection`.
 */
package ixdar.annotations.geometry;
